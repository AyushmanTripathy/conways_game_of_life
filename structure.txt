# start button clicked -

  startGame();
    return if already running

    init();
      calc each block's size

      start the interval (loop){

        check for game rules : gameLoop();
        draw()
        check if empty
        
      }